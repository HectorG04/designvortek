/**
 * Public site layout — wraps all marketing-facing pages.
 * Coming soon: no nav or footer yet (the coming-soon page is self-contained).
 * Will add Header + Footer here when designs land.
 */
export default function PublicLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
